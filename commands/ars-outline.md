---
description: Detailed paper outline + evidence map (no full draft)
model: sonnet
---

Invoke: `academic-paper` skill, mode: `outline`

Argument `$ARGUMENTS` is the topic or an existing Chapter Plan.
If no argument, ask for topic or paste a Chapter Plan from /ars-plan.

Execute Phases 0–2 from academic-paper/SKILL.md only.
Output: section outline + subsection headings + evidence map + gap analysis.
